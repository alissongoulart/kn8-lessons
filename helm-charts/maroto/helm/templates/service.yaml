apiVersion: v1
kind: Service
metadata:
  name: {{ include "maroto.fullname" . }}
  labels:
    {{- include "maroto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maroto.selectorLabels" . | nindent 4 }}
